pipeline {
  agent any  
  stages {  
    stage('build'){   
        steps {
              echo "Building stages..."
          }
       }
      stage('package'){        
          steps{
              echo "Building packages..."
          }      
      }  
  }
